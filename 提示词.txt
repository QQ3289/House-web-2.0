你是一名经验丰富的qt 项目工程师，擅长编写基于qt 的 linux web服务器；
现在请你编写一个web项目，要求如下：
1、环境：在阿里云乌班图系统上运行，基于qt6，使用mysql数据库、webengine；
2、目的：提供二手房信息服务，支持用户注册、添加收藏、设置偏好、按(地区、价格、房产信息)进行筛选；
3、项目结构：使用mysql服务持久化数据、使用qt开发前端与后端、用webengine开发网页；
4、功能：
a、用户：支持游客模式、提供开发者账户(进行用户账户管理，按照用户画像查看统计)、支持用户账号注册{设置账号、密码、邮箱验证、修改密码}、注册时使用邮箱验证，修改密码时发送邮件提示；
b、房产功能：浏览房源(来自于mysql数据库)、房源筛选(支持按房价、单位价格、地区、面积)、查看详情(点击房源之后跳转到详细界面，了解所有该房源相关信息；于此同时调用百度地图api，显示这个房产的位置)、支持生成视图，查看房产的统计结果；
c、ai助手：接入deepseek的计费api接口，提供只能助手，实现以下功能:接受用户的需求，智能检索mysql库中的房产数据，提供购房推荐；
5、mysql中的表结构：(我已经创建好了房产数据表格，不改变这个表格，你可以创建其他需要的表格，【】中的内容是对表栏的说明)
数据库：house_db;
表格：houseinfo(ID【主键】 int 自增主键, houseTitle【房产名】 varchar(100), price【房产总价】 double, area【房产面积】 double, communityNa【小区名】 varchar(255), floor【房产楼层，可为空】 varchar(50), houseType【房型，如两室一厅】varchar(50) unitPrice【每平米的价格】double，houseUrl【数据来源网址】varchar(1024));
6、设计一个美观的ui；
7、将所有需要的配置：数据库、deepseek api、百度地图api 单独封装为一个json文件
8、请编写能够给在生产环境中使用的项目版本，并且告诉我需要哪些配置；
8、请你生成这个项目，同时生成且仅生成在阿里云ecs(乌班图系统)上部署的指南；
